/** 리턴 */

function getTimes(x, y) {       // Times일 경우 곱셈?
    const z = x * y;
    return z;
}

const a = getTimes(123, 45);
console.log(a);

const b = getTimes(123, 45) + 10000;
console.log(b);

console.log(getTimes(100, 20));